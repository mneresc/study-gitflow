name: ci-sample
on: [push]
jobs:
    check-app:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2 #baixa o repositório
            - uses: actions/setup-go@v2
              with:
                go-version: '1.19'
            - run: cd src && go test #run quando quero usar comando
            - run: cd src && go run math.go #run quando quero usar comando
